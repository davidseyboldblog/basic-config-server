server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: ${HOME}/configuration-files
          search-paths:
           - '{application}'